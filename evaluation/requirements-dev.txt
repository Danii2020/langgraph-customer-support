# Development/test dependencies for the RAG Evaluation Pipeline.
# These are NOT deployed to Lambda (boto3 is already in the Lambda runtime).
# Install with: pip install -r evaluation/requirements-dev.txt

pytest>=8.0.0
pytest-mock>=3.12.0
pytest-cov>=5.0.0
